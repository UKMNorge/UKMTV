{% extends "UKMDesign/Layout/Base.html.twig" %}

{% block content %}
	<div class="container">
		<div class="row">
			{% for film in filmer.getAll() %}
				<div class="col-6 col-md-4 col-lg-3 col-xl-2">
					{{ film.getTitle() }}
                </div>
			{% else %}
				<div class="col-12">Beklager, fant ingen filmer</div>
			{% endfor %}
		</div>
	</div>
{% endblock %}
